export type Screen = 'home' | 'setup' | 'practice' | 'result';
export type Student = { nickname: string; gender?: 'boy' | 'girl' };
export type Progress = { completed: string[]; lastLetter: string };
